**Install NodeJS**

sudo apt update
sudo apt install nodejs
node -v
sudo apt install npm

**Install PM2**

npm install pm2 -g
pm2 completion install

**Install Angular/CLI**

npm install -g @angular/cli
